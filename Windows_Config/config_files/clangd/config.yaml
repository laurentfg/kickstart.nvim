# Machine-wide clangd 22 defaults. Per-repo compile_commands.json owns SFML + MSVC STL.
If:
  PathMatch: .*\.(c|cc|cpp|cxx|h|hh|hpp|hxx)
CompileFlags:
  Add:
    - --target=x86_64-pc-windows-msvc
    - -std=c++17
